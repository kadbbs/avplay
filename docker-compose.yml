services:
  mediamtx:
    image: bluenviron/mediamtx:latest
    container_name: streamforge-mediamtx
    network_mode: host
    volumes:
      - ./configs/mediamtx.yml:/mediamtx.yml:ro
    restart: unless-stopped
